# Parameterized-Multi-Request-Elevator-Controller
A synthesizable RTL elevator controller for a 5-floor building, implemented as a Mealy/Moore-style FSM with a SCAN-algorithm scheduler (the same disk-scheduling strategy used in OS I/O schedulers, applied to floor requests).self-checking testbench, and GTKWave support
